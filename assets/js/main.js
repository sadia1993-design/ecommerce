(function($) {
    "use strict";

    jQuery(document).ready(function($) {

      // banner slider
     $('.slider-area').slick({
        autoplay: true,
        speed: 800,
        arrows:false,
        dots: true,
        fade:true
     });

    });

    
}(jQuery));